---
title: Callout
---

# Callout

Sử dụng chú thích để thêm ngữ cảnh bắt mắt vào nội dung của bạn.

<Tabs>
  <Tab title="Note">
    <Note>Đây là một ghi chú</Note>
    ```mdx
    <Note>Đây là một ghi chú</Note>
    ```
  </Tab>
  <Tab title="Warning">
    <Warning>Đây là một cảnh báo</Warning>
    ```mdx
    <Warning>Đây là một cảnh báo</Warning>
    ```
  </Tab>
  <Tab title="Info">
    <Info>Đây là một thông tin</Info>
    ```mdx
    <Info>Đây là một thông tin</Info>
    ```
  </Tab>
  <Tab title="Tip">
    <Tip>Đây là một tip</Tip>
    ```mdx
    <Tip>Đây là một tip</Tip>
    ```
  </Tab>
  <Tab title="Check">
    <Check>Đây là một trạng thái đã được đánh dấu</Check>
    ```mdx
    <Check>Đây là một trạng thái đã được đánh dấu</Check>
    ```
  </Tab>
</Tabs>
