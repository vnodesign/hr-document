# Ngôn ngữ lập trình Java

Nhắc đến ngôn ngữ lập trình backend thì không thể không kể đến Java. Đây là ngôn ngữ lập trình đa năng, đa nền tảng, hướng đối tượng (object-oriented) và có tính đồng thời (concurrent) được phát triển bởi Oracle.
